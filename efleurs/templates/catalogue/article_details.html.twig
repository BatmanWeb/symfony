{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
 <div class="text-center">
 <h1>{{article.artNom}}</h1>
 </div>

<div class="card mb-3">
  <div class="row g-0">
    <div class="col-md-4">
      <img src="{{ asset ('articles/' ~ article.artImage)}}" class="img-fluid rounded-start" alt="...">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h5 class="card-title">{{article.artNom}}</h5>
                <p class="card-text">{{article.artDescription}}</p>
    {#Prix du produit#}
    
                      <div class="card-group">
                    <div class="card border-success mb-3 " style="max-width:18rem;">
                     <div class="card-body">
                     <p class="card-text text-center h2 text-success">{{article.prixTTC | number_format(2)}} €</p>
                     <p class="card-text text-center">
                     <a class="btn btn-success" href="{{ path('panier-add',{'id' : article.id}) }}">Ajouter au panier</a>
                     </p>
                    </div>
                    </div>
                    </div>
                    {#----- Fin -----#}
</div>
      </div>
    </div>
  </div>
</div>

{% endblock %}